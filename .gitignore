# OS / editor / tooling
.DS_Store
.entire/
__pycache__/
*.pyc
.venv/
.coverage
htmlcov/
.idea/
.vscode/

# secrets — keys live in ~/.config/watch/.env, never in-repo
.env
!.env.example

# build artifact from scripts/build-skill.sh
/dist/

# local Claude Code settings
.claude/settings.local.json

# oh-my-openagent state (keep plans tracked)
.omo/*
!.omo/plans/
